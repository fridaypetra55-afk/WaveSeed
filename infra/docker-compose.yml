version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: waveseed
      POSTGRES_PASSWORD: password
      POSTGRES_DB: waveseed
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
