# Frontend (wavespeed)

This is a minimal Next.js + TypeScript + Tailwind scaffold. Install dependencies and run locally:

```bash
cd frontend
npm install
npm run dev
```

Connect a Stellar wallet (Freighter or web wallet) and use Stellar Wallet Kit to sign transactions interacting with Soroban contracts.
